import { times } from "lodash"; // This is a JavaScript library, no TypeScript
times(4);

// .d.ts files are declaration files that contain type information for JavaScript code.
// They are used by the TypeScript compiler to type-check our code.

console.myLog("Hello, world!");

test
window.test
